node_modules
dist
.env.local
.DS_Store
test-results/
.playwright-browsers/

# Private source-mapped question bank (local only — never commit)
data/source-question-bank/*
!data/source-question-bank/README.md

# OneNote export copies (source lives in ~/Documents/markdown)
data/onenote/raw/
data/onenote/normalized/

# Batch-claim coordination state (see ai-improvement-logs/BATCH_CLAIM_99.md) — ephemeral,
# per-machine, never meant to be committed
ai-improvement-logs/batch_claims.json
ai-improvement-logs/.batch_claims.lock
